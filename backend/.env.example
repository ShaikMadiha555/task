# XAMPP default MySQL: user root, empty password, port 3306
# Create database `taskapp` in phpMyAdmin first, then run: npx prisma migrate deploy
DATABASE_URL="mysql://root:@localhost:3306/taskapp"
JWT_SECRET="replace-with-long-random-string-in-production"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
