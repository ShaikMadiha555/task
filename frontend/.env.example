# Optional: public API base when the UI is not served from the same origin.
# Development: leave unset to use the Vite dev proxy for /api.
# Production: e.g. VITE_API_URL=https://your-api.example.com
# VITE_API_URL=
